{
  "settings": [
    {
      "name": "dafault_current_folder",
      "value": false,
      "description": "Always use current folder as default",
      "info": "If true, clir will get/save/delete commands from the folder where it was executed. For this to work you need to create a local project by adding the first command using the --local/-l flag."
    }
  ]
}
